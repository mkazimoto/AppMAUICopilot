// Add the following constant inside the ApiConfig.Endpoints static class,
// alongside the existing Forms constant in Config/ApiConfig.cs:

public static class Endpoints
{
    // ... existing constants ...

    /// <summary>The relative path to the categories resource endpoint.</summary>
    public const string Categories = "/api/v1/categories";
}
