<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="CameraApp.Views.CameraPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:CameraApp.ViewModels"
             xmlns:services="clr-namespace:CameraApp.Services"
             Title="{Binding [Camera_AppTitle], Source={x:Static services:LocalizationResourceManager.Instance}}"
             x:DataType="vm:CameraPageViewModel">
    
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">
            
            <!-- Título -->
            <Label Text="{Binding [Camera_AppTitle], Source={x:Static services:LocalizationResourceManager.Instance}}"
                   FontSize="24"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   TextColor="{StaticResource Primary}" />
            
            <!-- Botões de ação -->
            <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                <Button Grid.Column="0"
                        Text="{Binding [Camera_TakePhoto], Source={x:Static services:LocalizationResourceManager.Instance}}"
                        Command="{Binding TakePhotoCommand}"
                        BackgroundColor="{StaticResource Primary}"
                        TextColor="{StaticResource White}"
                        AutomationId="TakePhotoButton"
                        SemanticProperties.Description="{Binding [Accessibility_TakePhoto], Source={x:Static services:LocalizationResourceManager.Instance}}"
                        CornerRadius="8" />
                
                <Button Grid.Column="1"
                        Text="{Binding [Camera_Gallery], Source={x:Static services:LocalizationResourceManager.Instance}}"
                        Command="{Binding PickPhotoCommand}"
                        BackgroundColor="{StaticResource Secondary}"
                        TextColor="{StaticResource White}"
                        AutomationId="PickPhotoButton"
                        SemanticProperties.Description="{Binding [Accessibility_SelectFromGallery], Source={x:Static services:LocalizationResourceManager.Instance}}"
                        CornerRadius="8" />
            </Grid>
            
            <!-- Área da foto -->
            <Border IsVisible="{Binding HasPhoto}"
                    BackgroundColor="{StaticResource Gray200}"
                    Padding="0"
                    StrokeThickness="0">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="10"/>
                </Border.StrokeShape>
                
                <VerticalStackLayout Spacing="10">
                    <!-- Imagem -->
                    <Image Source="{Binding PhotoPath}"
                           Aspect="AspectFit"
                           HeightRequest="300"
                           HorizontalOptions="Fill"
                           AutomationId="PhotoImage"
                           SemanticProperties.Description="{Binding [Accessibility_SelectedPhoto], Source={x:Static services:LocalizationResourceManager.Instance}}" />
                    
                    <!-- Botão para limpar -->
                    <Button Text="{Binding [Camera_ClearPhoto], Source={x:Static services:LocalizationResourceManager.Instance}}"
                            Command="{Binding ClearPhotoCommand}"
                            BackgroundColor="{StaticResource Danger}"
                            TextColor="{StaticResource White}"
                            AutomationId="ClearPhotoButton"
                            SemanticProperties.Description="{Binding [Accessibility_RemovePhoto], Source={x:Static services:LocalizationResourceManager.Instance}}"
                            CornerRadius="8"
                            Margin="10" />
                </VerticalStackLayout>
            </Border>
            
            <!-- Mensagem quando não há foto -->
            <Border IsVisible="{Binding HasPhoto, Converter={StaticResource InvertedBoolConverter}}"
                    BackgroundColor="{StaticResource Gray200}"
                    HeightRequest="300"
                    Padding="20"
                    StrokeThickness="0">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="10"/>
                </Border.StrokeShape>
                
                <Label Text="{Binding [Camera_NoPhotoMessage], Source={x:Static services:LocalizationResourceManager.Instance}}"
                       FontSize="16"
                       TextColor="{StaticResource Gray500}"
                       HorizontalTextAlignment="Center"
                       VerticalTextAlignment="Center" />
            </Border>

            <!-- Indicador de carregamento -->
            <ActivityIndicator IsVisible="{Binding IsLoading}"
                               IsRunning="{Binding IsLoading}"
                               Color="{StaticResource Primary}"
                               HorizontalOptions="Center" />
            
        </VerticalStackLayout>
    </ScrollView>
    
</ContentPage>