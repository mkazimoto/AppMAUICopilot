<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="CameraApp.Views.FormEditPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:CameraApp.ViewModels"
             xmlns:services="clr-namespace:CameraApp.Services"
             x:DataType="viewmodels:FormEditViewModel"
             Title="{Binding PageTitle}">

    <Shell.BackButtonBehavior>
        <BackButtonBehavior IsEnabled="{Binding IsLoading, Converter={StaticResource InvertedBoolConverter}}"/>
    </Shell.BackButtonBehavior>

    <ScrollView>
        <StackLayout Padding="20"
                Spacing="20">

            <!-- Header -->
            <Border BackgroundColor="{StaticResource Primary}"
                    Padding="20"
                    StrokeShape="RoundRectangle 10">
                <VerticalStackLayout>
                    <Label Text="{Binding PageTitle}"
                           FontSize="20"
                           FontAttributes="Bold"
                           TextColor="{StaticResource White}"
                           HorizontalOptions="Center"
                           SemanticProperties.HeadingLevel="Level2"/>
                    <Label Text="{Binding [FormEdit_Subtitle], Source={x:Static services:LocalizationResourceManager.Instance}}"
                           FontSize="14"
                           TextColor="{StaticResource White}"
                           HorizontalOptions="Center"
                           Margin="0,5,0,0"/>
                </VerticalStackLayout>
            </Border>

            <!-- Título -->
            <Border BackgroundColor="{StaticResource White}"
                    Padding="15"
                    StrokeShape="RoundRectangle 8">
                <VerticalStackLayout>
                    <Label Text="{Binding [FormEdit_TitleField], Source={x:Static services:LocalizationResourceManager.Instance}}"
                           FontSize="16"
                           FontAttributes="Bold"
                           TextColor="{StaticResource Primary}"
                           SemanticProperties.HeadingLevel="Level2"/>
                    <Entry Text="{Binding Title}"
                           Placeholder="{Binding [FormEdit_TitlePlaceholder], Source={x:Static services:LocalizationResourceManager.Instance}}"
                           FontSize="14"
                           AutomationId="TitleEntry"
                           Margin="0,5,0,0"/>
                </VerticalStackLayout>
            </Border>

            <!-- Categoria e Status -->
            <Border BackgroundColor="{StaticResource White}"
                    Padding="15"
                    StrokeShape="RoundRectangle 8">
                <Grid ColumnDefinitions="*,*"
                        ColumnSpacing="10">
                    <VerticalStackLayout Grid.Column="0">
                        <Label Text="{Binding [FormEdit_Category], Source={x:Static services:LocalizationResourceManager.Instance}}"
                               FontSize="16"
                               FontAttributes="Bold"
                               TextColor="{StaticResource Primary}"
                               SemanticProperties.HeadingLevel="Level2"/>
                        <Picker ItemsSource="{Binding Categories}"
                                SelectedIndex="{Binding CategoryId}"
                                ItemDisplayBinding="{Binding Name}"
                                FontSize="14"
                                AutomationId="CategoryPicker"
                                Margin="0,5,0,0"/>
                    </VerticalStackLayout>

                    <VerticalStackLayout Grid.Column="1">
                        <Label Text="{Binding [FormEdit_Status], Source={x:Static services:LocalizationResourceManager.Instance}}"
                               FontSize="16"
                               FontAttributes="Bold"
                               TextColor="{StaticResource Primary}"
                               SemanticProperties.HeadingLevel="Level2"/>
                        <Picker ItemsSource="{Binding StatusItems}"
                                SelectedIndex="{Binding StatusFormId}"
                                ItemDisplayBinding="{Binding Name}"
                                FontSize="14"
                                AutomationId="StatusPicker"
                                Margin="0,5,0,0"/>
                    </VerticalStackLayout>
                </Grid>
            </Border>

            <!-- Pontuação Total -->
            <Border BackgroundColor="{StaticResource White}"
                    Padding="15"
                    StrokeShape="RoundRectangle 8">
                <VerticalStackLayout>
                    <Label Text="{Binding [FormEdit_Score], Source={x:Static services:LocalizationResourceManager.Instance}}"
                           FontSize="16"
                           FontAttributes="Bold"
                           TextColor="{StaticResource Primary}"
                           SemanticProperties.HeadingLevel="Level2"/>
                    <Entry Text="{Binding TotalScore}"
                           Placeholder="0"
                           Keyboard="Numeric"
                           FontSize="14"
                           AutomationId="ScoreEntry"
                           Margin="0,5,0,0"/>
                    <Label Text="{Binding [FormEdit_ScoreHint], Source={x:Static services:LocalizationResourceManager.Instance}}"
                           FontSize="12"
                           TextColor="{StaticResource Gray600}"
                           Margin="0,5,0,0"/>
                </VerticalStackLayout>
            </Border>

            <!-- Configurações -->
            <Border BackgroundColor="{StaticResource White}"
                    Padding="15"
                    StrokeShape="RoundRectangle 8">
                <VerticalStackLayout>
                    <Label Text="{Binding [Posture_Settings], Source={x:Static services:LocalizationResourceManager.Instance}}"
                           FontSize="16"
                           FontAttributes="Bold"
                           TextColor="{StaticResource Primary}"
                           SemanticProperties.HeadingLevel="Level2"
                           Margin="0,0,0,10"/>

                    <Grid ColumnDefinitions="Auto,*"
                            ColumnSpacing="10">
                        <Switch Grid.Column="0"
                                IsToggled="{Binding SequentialScript}"
                                VerticalOptions="Center"
                                AutomationId="SequentialScriptSwitch"/>
                        <VerticalStackLayout Grid.Column="1"
                                VerticalOptions="Center">
                            <Label Text="{Binding [FormEdit_SequentialScript], Source={x:Static services:LocalizationResourceManager.Instance}}"
                                   FontSize="14"
                                   FontAttributes="Bold"
                                   TextColor="{StaticResource Gray900}"/>
                            <Label Text="{Binding [FormEdit_SequentialScriptHint], Source={x:Static services:LocalizationResourceManager.Instance}}"
                                   FontSize="12"
                                   TextColor="{StaticResource Gray600}"/>
                        </VerticalStackLayout>
                    </Grid>
                </VerticalStackLayout>
            </Border>

            <!-- Informações de ID (apenas no modo de edição) -->
            <Border BackgroundColor="{StaticResource Gray100}"
                    Padding="20"
                    StrokeShape="RoundRectangle 10"
                    IsVisible="{Binding IsEditMode}">
                <VerticalStackLayout>
                    <Label Text="{Binding [FormEdit_SystemInfo], Source={x:Static services:LocalizationResourceManager.Instance}}"
                           FontSize="16"
                           FontAttributes="Bold"
                           TextColor="{StaticResource Gray700}"
                           SemanticProperties.HeadingLevel="Level2"
                           Margin="0,0,0,10"/>

                    <Grid ColumnDefinitions="Auto,*"
                            RowDefinitions="Auto,Auto"
                            ColumnSpacing="10"
                            RowSpacing="5">
                        <Label Grid.Row="0"
                                Grid.Column="0"
                               Text="{Binding [FormEdit_IdLabel], Source={x:Static services:LocalizationResourceManager.Instance}}"
                               FontSize="12"
                               FontAttributes="Bold"
                               TextColor="{StaticResource Gray600}"/>
                        <Label Grid.Row="0"
                                Grid.Column="1"
                               Text="{Binding FormId}"
                               FontSize="12"
                               TextColor="{StaticResource Gray700}"/>
                    </Grid>
                </VerticalStackLayout>
            </Border>

            <!-- Botão de Salvar -->
            <Button Text="{Binding IsEditMode, Converter={StaticResource EditModeToTextConverter}}"
                    Command="{Binding SaveCommand}"
                    BackgroundColor="{StaticResource Primary}"
                    TextColor="{StaticResource White}"
                    FontSize="16"
                    FontAttributes="Bold"
                    HeightRequest="50"
                    AutomationId="SaveButton"
                    SemanticProperties.Description="{Binding [Accessibility_SaveForm], Source={x:Static services:LocalizationResourceManager.Instance}}"
                    IsEnabled="{Binding IsLoading, Converter={StaticResource InvertedBoolConverter}}"/>

            <!-- Botões de ação secundária -->
            <Grid ColumnDefinitions="*,*,*"
                    ColumnSpacing="10">

                <!-- Botão Excluir (apenas no modo de edição) -->
                <Button Grid.Column="0"
                        Text="{Binding [Common_Delete], Source={x:Static services:LocalizationResourceManager.Instance}}"
                        Command="{Binding DeleteCommand}"
                        BackgroundColor="{StaticResource Secondary}"
                        TextColor="{StaticResource Primary}"
                        FontSize="14"
                        HeightRequest="45"
                        AutomationId="DeleteButton"
                        SemanticProperties.Description="{Binding [Accessibility_DeleteForm], Source={x:Static services:LocalizationResourceManager.Instance}}"
                        IsVisible="{Binding CanDelete}"
                        IsEnabled="{Binding IsLoading, Converter={StaticResource InvertedBoolConverter}}"/>

                <!-- Botão Cancelar -->
                <Button Grid.Column="2"
                        Text="{Binding [Common_Cancel], Source={x:Static services:LocalizationResourceManager.Instance}}"
                        Command="{Binding CancelCommand}"
                        BackgroundColor="Transparent"
                        TextColor="{StaticResource Gray600}"
                        BorderColor="{StaticResource Gray300}"
                        BorderWidth="1"
                        FontSize="14"
                        HeightRequest="45"
                        AutomationId="CancelButton"
                        SemanticProperties.Description="{Binding [Accessibility_CancelEdit], Source={x:Static services:LocalizationResourceManager.Instance}}"
                        IsEnabled="{Binding IsLoading, Converter={StaticResource InvertedBoolConverter}}"/>
            </Grid>

            <!-- Loading indicator -->
            <StackLayout IsVisible="{Binding IsLoading}"
                         VerticalOptions="Center"
                         HorizontalOptions="Center"
                         Padding="20">
                <ActivityIndicator IsRunning="{Binding IsLoading}"
                                   Color="{StaticResource Primary}"
                                   WidthRequest="40"
                                   HeightRequest="40"/>
                <Label Text="{Binding [Common_Processing], Source={x:Static services:LocalizationResourceManager.Instance}}"
                       FontSize="16"
                       TextColor="{StaticResource Primary}"
                       HorizontalOptions="Center"
                       Margin="0,10,0,0"/>
            </StackLayout>

        </StackLayout>
    </ScrollView>
</ContentPage>